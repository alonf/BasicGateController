<?xml version="1.0" encoding="utf-8" ?>
<forms:ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.Core"
             x:Class="BasicGateApp.MainPage">
    <StackLayout>
        <Frame HasShadow="True" OutlineColor="Black">
            <Button Text="Toggle Blue LED"
                FontSize="48"
                HorizontalOptions="Center"
                VerticalOptions="CenterAndExpand"
                Clicked="OnToggleBlueLed"/>
        </Frame>
        <Frame HasShadow="True" OutlineColor="Black">
            <StackLayout Orientation="Vertical">
            <Button Text="Open Gate"
                FontSize="48"
                HorizontalOptions="Center"
                VerticalOptions="CenterAndExpand"
                Clicked="OnOpenMiniatureGate"/>
                <Button Text="Close Gate"
                FontSize="48"
                HorizontalOptions="Center"
                VerticalOptions="CenterAndExpand"
                Clicked="OnCloseMiniatureGate"/>
                <Button Text="Stop Gate"
                FontSize="48"
                HorizontalOptions="Center"
                VerticalOptions="CenterAndExpand"
                Clicked="OnStopMiniatureGate"/>
            </StackLayout>
        </Frame>
    </StackLayout>
</forms:ContentPage>