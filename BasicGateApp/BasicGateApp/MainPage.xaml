<?xml version="1.0" encoding="utf-8" ?>
<forms:ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.Core"
             x:Class="BasicGateApp.MainPage">
    <StackLayout>
        <Button Text="Activate Fun Propellor"
            FontSize="36"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            Clicked="OnActivateFunPropellor"/>
        <StackLayout Orientation="Horizontal">
        <Button Text="Open Miniature Gate"
            FontSize="36"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            Clicked="OnOpenMiniatureGate"/>
            <Button Text="Close Miniature Gate"
            FontSize="36"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            Clicked="OnCloseMiniatureGate"/>
            <Button Text="Stop Miniature Gate"
            FontSize="36"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            Clicked="OnStopMiniatureGate"/>
        </StackLayout>
    </StackLayout>
</forms:ContentPage>